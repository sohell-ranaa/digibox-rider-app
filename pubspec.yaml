name: digibox_rider_tracking_app
description: "Digibox Rider - NextGen last-mile delivery tracking solution"
publish_to: 'none'

version: 1.2.11+21

environment:
  sdk: ^3.5.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.2

  # HTTP & API
  http: ^1.2.0

  # Phase 1: Security & Error Handling
  retry: ^3.1.2                    # Retry logic with exponential backoff
  logger: ^2.0.2                   # Structured logging

  # Location & Background Services
  geolocator: ^10.1.0
  permission_handler: ^11.3.0
  workmanager: ^0.5.2
  flutter_foreground_task: ^7.1.1  # Foreground service for reliable tracking
  flutter_local_notifications: ^17.0.0  # Persistent notification

  # Local Storage
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  path_provider: ^2.1.2

  # Connectivity
  connectivity_plus: ^5.0.2

  # Phase 2: UI Components
  shimmer: ^3.0.0                  # Skeleton loading

  # Maps
  flutter_map: ^6.1.0              # OpenStreetMap (FREE)
  latlong2: ^0.9.0                 # Lat/Long calculations

  # AI/ML for GPS Processing
  ml_linalg: ^13.12.7              # Linear algebra for Kalman filter
  ml_dataframe: ^1.4.0             # Data processing

  # Firebase (disabled - backend not implemented)
  # firebase_core: ^2.24.2
  # firebase_messaging: ^14.7.10

  # Utilities
  intl: ^0.19.0
  path: ^1.8.3                     # Path manipulation

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # App assets
  assets:
    - assets/icons/app_icon.png

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icons/app_icon.png"
  adaptive_icon_background: "#1976D2"
  adaptive_icon_foreground: "assets/icons/app_icon.png"

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
